"""Shared utilities for the Unscreen codebase.

Pure helpers only — modules here must not import from ``core`` or ``UI``.
Import from sibling submodules directly (e.g. ``utils.paths``), never from
this package root.
"""
